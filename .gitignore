# 원본 데이터 (용량이 크고 다시 받을 수 있음)
back_data/
current_data/
사업자_기본정보(주유소)/
Claude outputs/

# DB - 매일 바뀌므로 저장소에 넣지 않는다 (빌드는 로컬에서 한다)
scripts/oil.db
scripts/oil.db.bak
scripts/*.db-journal

# 파이썬
__pycache__/
*.pyc
.venv/

# 작업 중 산출물
*.log
.DS_Store
Thumbs.db

# 미리보기용 데이터 복사본 (api/ 와 같은 내용이라 저장소에 두 번 넣지 않는다)
.theme-preview/api/

# 열쇠 - 절대 올리지 않는다 (카카오 REST 키는 도메인 제한이 안 걸린다)
scripts/local_keys.py

# 빌드 결과물 - 키가 박혀 나오므로 저장소에 두지 않는다. build_theme.py 로 다시 만든다
blogger/theme-oil.xml
.theme-preview/
